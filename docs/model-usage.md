# Model usage ledger

One concise row per completed implementation/review/testing/research session.
Future assignments live only in `docs/ROADMAP.md`; the canonical routing policy
lives only in `docs/ARCHITECTURE.md`. This ledger records what was requested
and what actually ran, never a generic tier label.

Rows before the 2026-07-14 routing reset remain in Git history at commit
`603b17b`. They were removed from the active preview because their ambiguous
`Terra/Sol` labels and repeated `GPT-5-based host` wording did not prove an
actual model deployment and included superseded product work.

| Date | Stage | Work | Role | Requested Codex route | Actual host | Substitution / escalation | Verification evidence |
|---|---|---|---|---|---|---|---|
| 2026-07-15 | S4 Portfolio identity repair | unified new/reused sync identity resolution, safe minimal GPW-company creation, immutable snapshot repair semantics, manual rationale and visible ambiguity reasons | bounded implementer / mapping design auditor / independent read-only verifier / browser acceptance drafter | `gpt-5.6-terra` medium for implementation; `gpt-5.6-sol` high for strict verification | host deployment not exposed | collaboration contexts did not expose deployment identity; no Research or valuation job was created or claimed by this slice | the verifier rejected the first pass, then direct probes proved the repaired existing-company ETF/USD/GPW boundary and stripped-rationale gate; 49 Portfolio tests, 10 Vision tests, production build and mapping-reason browser rendering pass. The live myfund request failed honestly before re-resolution; automatic coverage was audited but intentionally retained as a separate queue/schema slice. |
| 2026-07-15 | S4 Portfolio return precision | replaced hardcoded unavailable returns with one deterministic TWR/XIRR contract, visible method/window/gaps and DB-bound review recomputation | bounded implementer / arithmetic auditor / independent strict verifier / browser acceptance drafter | no model for math; `gpt-5.6-terra` medium for implementation; `gpt-5.6-sol` high for strict verification | host deployment not exposed | collaboration contexts did not expose a deployment identity; no queued company artifact model was claimed by this slice | independent live recomputation matched 193.079779% TWR and 41.120758% ACT/365 XIRR over 1,260 days/47 flows; verifier found and closed clustered-root and frozen-history lineage defects; 44 Portfolio tests, 10 Vision tests, production build and live Portfolio cards/provider-label browser checks pass. Cross-stage smoke remains pending overall because the rehydrated Discover batch still shows SNT #5/80.7 while its owner-authorized Research rebuild has a live lease. |
| 2026-07-14 | ROUTING-RESET / S2–S5 | merged failed Research/Valuation acceptance and the clean-baseline rebuild into the one Roadmap; aligned model policy and added mandatory browser-first Vision verification | manager / policy implementer / browser acceptance drafter | `gpt-5.6-sol` high for product/architecture reconciliation; future task routes are recorded exactly in the Roadmap | host deployment not exposed | current interactive host could not be proven to equal the requested route; no model identity was inferred; this context created the acceptance skill and therefore cannot issue the future independent implementation verdict | official model guidance inspected; 342 backend regressions and production build passed as supporting evidence, but live browser acceptance failed S2–S3: metadata-led Research, all-at-once company detail, assumption-first Valuation and visible ABS 30/50/20; Roadmap now requires legacy-code deletion, one regenerated baseline migration, empty-DB rebuild and fresh browser acceptance before Portfolio resumes |
| 2026-07-14 | S2–S5 recovery | deleted legacy current-read/model/schema paths; generated the one canonical baseline; recreated local PostgreSQL and rebuilt Discover plus the stored Portfolio snapshot | bounded implementation / schema reset / browser verifier | `gpt-5.6-terra` medium for bounded implementation; `gpt-5.3-codex-spark` fallback for mechanical checks; independent `gpt-5.6-sol` high remains required for artifact acceptance | host deployment not exposed | no host identity was inferred and no Research/Valuation model job was claimed; a user-selected company is still required before cost-bearing queue work | 341 backend tests, production build, PostgreSQL `alembic check`, no retired tables/columns, fresh 7-page Discover batch, Portfolio re-sync and browser checks of single-sieve Discover plus the honest empty Research/Valuation states |
| 2026-07-14 | S2–S5 / SNT | executed SNT through the frozen Research v3 and Valuation v2 queue from Discover handoff to an empty queue | Research drafter; Research strict verifier; Valuation drafter; Valuation strict verifier | Research `gpt-5.6-terra` high; Valuation and both independent verifier routes `gpt-5.6-sol` high | host deployment not exposed | requested route retained in AgentRuns/verifier rows; interactive host identity is not exposed and was recorded explicitly rather than inferred | Research verification: 9-source normalized manifest, 38 statement-provenance paths, 11 named gaps, one minor gross-margin finding; Valuation structural gates all passed and strict verifier retained two minor limitations (gross-margin ambiguity; raw-price/market-cap mismatch). Canonical snapshots v1 are provisional, queue is empty, and the browser confirms Discover → Research → Valuation → Portfolio. |
| 2026-07-14 | S2–S3 / SNT economic re-review | independently split SNT primary research, valuation-method diagnosis and strict re-verification after the owner rejected the first artifact; added exact-URL issuer authorization, corrected the shared user-agent policy and made probability rationale visible | primary researcher / valuation specialist / independent strict verifier / bounded implementation | Research `gpt-5.6-terra` high requested; valuation and strict verification `gpt-5.6-sol` high requested; implementation `gpt-5.6-terra` medium requested | host deployment not exposed | three independent contexts were used as requested; no context claimed the requested deployment as the actual host | Official evidence disproved the saved business perimeter and cash interpretation; deterministic recomputation proved BiznesRadar C/Z recreated the current market-cap-implied price and the old base double-discounted earnings and multiple. Issuer index plus H1/ESPI 23/28/36 are retained after the user-agent fix. Focused issuer/HTTP tests, Vision gate, production build and browser verification pass for the implementation; economic acceptance still fails until the owner confirms the replacement profile and a new independently verified draft exists. |
| 2026-07-15 | S2–S3 / SNT expectation-led rebuild | retained BiznesRadar consensus as the Discover/valuation baseline; rebuilt Research and valuation through adversarial repair; added five-year/stub FCFF, independent relative methods, sensitivity, reverse expectations, uncalibrated-probability non-publication and source-bound Discover normalization for material discontinued results | Research drafter / valuation specialist / independent strict verifier / bounded implementation / browser acceptance drafter | Research `gpt-5.6-terra` high; valuation and strict verification `gpt-5.6-sol` high; implementation `gpt-5.6-terra` medium | host deployment not exposed | three independent contexts were used; Research first failed three majors, valuation Run 4 was rejected, and the post-pass verifier then found the cross-stage Discover contradiction before repaired Research v2, valuation Run 5 and batch v6 passed; no requested deployment was claimed as the actual host | SNT source v36 matches 2026–2028 levels/growth/ranges/counts; batch #2 independently recomputes continuing growth 80.6217%, P/E 21.7714x, rank #26/63.2 and fingerprint `983c6d…`; valuation fingerprints, five-period/stub, method, reverse-DCF and null-probability math pass. The obsolete engine-v2 row is deleted; 347 backend tests, production build, runtime/API checks and live Discover → Research → company → Valuation → Portfolio acceptance pass, including 290.71 PLN base DCF, 244.38–360.06 PLN base sensitivity and +34.69% reverse-DCF revenue versus Street. |
| 2026-07-15 | S2–S5 baseline repair | repaired adversarially found full-vector/event valuation gates; deleted remaining retired schema/models; regenerated the sole baseline and rebuilt the disposable local DB | bounded implementer / independent verifier | deterministic code and migration work; no model requested | host deployment not exposed | separate read-only verifier found the defects, then recomputed the repaired gate and regenerated schema without editing | full 344-test backend suite, production build, Vision gate, SQLite and PostgreSQL Alembic parity, fresh Discover batch and myfund Portfolio sync pass; canonical Research/Valuation remain empty pending Kuba's renewed authorization. |
| 2026-07-15 | S2–S3 potential bridge | authenticated lead review of eight PortalAnaliz portfolio threads; designed and implemented the one evidence-bound driver→forecast→value bridge | source-mechanics researcher / bounded valuation implementer / independent implementation verifier | interactive source synthesis and implementation; no queued artifact model or cost-bearing company valuation was authorized | host deployment not exposed | forum return figures were treated as self-reported leads; investor identities and execution rules were excluded under V2/V9; exact host identity was not inferred | strong benchmark-relative evidence was found in topics 569, 272, 356, 509 and 1492, with high absolute results in 575 and weaker sustained-alpha evidence in 526/600; canonical v4 now computes exact driver reconciliation, NOPAT/reinvestment/ROIC terminal value and price hurdles; the independent pass found and closed driver-key and driver-label lineage defects; 361 backend tests, production build, migration parity and the honest-empty Discover → Research → Valuation → Portfolio browser smoke pass. A representative company artifact still requires explicit authorization. |
